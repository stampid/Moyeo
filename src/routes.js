// user
const USERS = "/users";
const SIGNUP = "/signup";
const LOGIN = "/login";
const LOGOUT = "/logout";

// room
const ROOMS = "/rooms";

// schedule
const SCHEDULES = "/schedules";

// kakao

// github

const routes = {
  users: USERS,
  signup: SIGNUP,
  login: LOGIN,
  logout: LOGOUT,
  rooms: ROOMS,
  schedules: SCHEDULES
};

export default routes;
